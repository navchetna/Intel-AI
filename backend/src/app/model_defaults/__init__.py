"""Model defaults module.

Stores the global, app-wide default model chosen per task type (OCR, Classification,
Reason, Generate, ...). Manually curated today; the same rows are the override slot
for a future LLM-suggested default, editable at any time by a Presales architect.
"""
