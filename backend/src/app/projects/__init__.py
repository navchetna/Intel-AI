"""Sizing projects module.

Stores named "Projects" — the persistence unit for a sizing session. Each
project holds a single JSON blob capturing the user's selections and sizing
inputs from both the Agentic Stack page and the Model Catalog page, so a
session can be saved, reopened, and continued from any browser.
"""
