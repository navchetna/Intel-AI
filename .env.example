# Copy to .env and adjust. Used by docker-compose.
POSTGRES_USER=intelai
POSTGRES_PASSWORD=intelai
POSTGRES_DB=intelai

# Host port mappings (change if a port is already in use).
DB_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=3000
BLUELENS_PORT=3013
BLUELENS_PORT=3003
