# Public API base URL used by the browser/SSR fetch layer.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8040

# For local development (no nginx proxy), set these to empty strings:
# ASSET_PREFIX=""
# NEXT_PUBLIC_BASE_PATH=""
